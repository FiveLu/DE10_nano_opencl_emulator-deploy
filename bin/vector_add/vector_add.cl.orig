__kernel void vector_add(__global  float *restrict a, 
                         __global  float *restrict b, 
                         __global float *restrict c)
{
    for(int i=0;i<10;i++)
        c[i]=a[i]+b[i];
}

